page {
  background: #F4F5F7;
  font-family: -apple-system, sans-serif;
}

.page-container {
  padding: 30rpx 30rpx 220rpx;
  display: flex;
  flex-direction: column;
  gap: 24rpx;
}

/* 基础卡片样式 */
.card {
  background: #FFFFFF;
  border-radius: 32rpx;
  box-shadow: 0 8rpx 30rpx rgba(0, 0, 0, 0.03);
  box-sizing: border-box;
}

/* --- 需求3：分贝区整块变色样式 --- */
.dashboard {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 50rpx 40rpx;
  transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  border: 1rpx solid rgba(0,0,0,0.05);
}

/* 状态颜色：安全 */
.detail-safe { background-color: #E8F5E9 !important; }
.detail-safe .status-pill { background: rgba(70, 221, 70, 0.2); }
.detail-safe .status-text { color: #2E7D32; }

/* 状态颜色：中风险 */
.detail-medium { background-color: #FFF3E0 !important; }
.detail-medium .status-pill { background: rgba(255, 184, 25, 0.2); }
.detail-medium .status-text { color: #E65100; }

/* 状态颜色：高风险（文字反白） */
.detail-high { background-color: #A41F35 !important; }
.detail-high .db-num, 
.detail-high .db-unit, 
.detail-high .tag-text,
.detail-high .stats-label { color: #FFFFFF !important; }
.detail-high .status-pill { background: rgba(255, 255, 255, 0.2); }
.detail-high .status-text { color: #FFFFFF; }
.detail-high .dot { background: #FFFFFF !important; box-shadow: 0 0 15rpx #fff; }

.recording-tag { display: flex; align-items: center; gap: 12rpx; margin-bottom: 20rpx; }
.tag-text { font-size: 22rpx; color: #888; font-weight: bold; letter-spacing: 2rpx; }
.dot { width: 12rpx; height: 12rpx; background: #ff3b30; border-radius: 50%; animation: pulse 1.5s infinite; }

.main-value { display: flex; align-items: baseline; }
.db-num { font-size: 110rpx; font-weight: 800; color: #111; font-variant-numeric: tabular-nums; }
.db-unit { font-size: 32rpx; color: #666; font-weight: 600; margin-left: 10rpx; }

.status-pill { padding: 8rpx 36rpx; border-radius: 100rpx; margin-top: 20rpx; }
.status-text { font-size: 28rpx; font-weight: bold; }

/* 辅助数据行 */
.stats-row { display: flex; gap: 24rpx; }
.stats-card { flex: 1; padding: 30rpx; }
.stats-label { font-size: 22rpx; color: #999; display: block; margin-bottom: 12rpx; }
.stats-value { display: flex; align-items: baseline; }
.stats-value .val { font-size: 44rpx; font-weight: 700; color: #333; font-variant-numeric: tabular-nums; }
.stats-value .unit { font-size: 20rpx; color: #AAA; margin-left: 6rpx; }

/* --- 需求1：图表居中控制 --- */
.chart-card { padding-bottom: 30rpx; }
.chart-header { padding: 30rpx 40rpx; }
.chart-title { font-size: 24rpx; color: #90a4ae; font-weight: 600; }

.canvas-container {
  width: 100%;
  height: 600rpx;
  display: flex;         /* 开启 flex */
  justify-content: center; /* 水平居中 */
  align-items: center;     /* 垂直居中 */
  background: #fff;
  position: relative;
}
/* canvas-layer 不再使用 absolute 以配合 flex 居中，或者手动指定 canvas 大小 */
.canvas-layer {
  width: 620rpx;  /* 对应 JS 中的 globalSize */
  height: 600rpx;
}

/* --- 需求2：底部操作栏比例调整 --- */
/* 底部操作栏容器 */
.action-bar {
  position: fixed;
  bottom: 0; left: 0; right: 0;
  background: rgba(255, 255, 255, 0.96);
  backdrop-filter: blur(20px);
  padding: 24rpx 30rpx calc(24rpx + env(safe-area-inset-bottom));
  display: flex;
  align-items: center;
  gap: 20rpx; /* 元素间距 */
  box-shadow: 0 -4rpx 30rpx rgba(0,0,0,0.05);
  z-index: 100;
}

/* 缩小后的位置按钮：小巧的正方形 */
.btn-mini {
  width: 100rpx;
  height: 100rpx;
  background: #F4F5F7;
  border-radius: 20rpx;
  display: flex;
  justify-content: center;
  align-items: center;
  flex-shrink: 0; /* 核心：防止被右侧按钮挤压变形 */
}

.mini-icon {
  width: 48rpx;
  height: 48rpx;
  opacity: 0.7;
}

.btn-hover-mini {
  background: #EAECEF;
}

/* 主操作按钮：平分剩余所有空间 */
.btn-main {
  flex: 1; /* 核心：平分剩余宽度 */
  height: 100rpx;
  border-radius: 24rpx;
  display: flex;
  justify-content: center;
  align-items: center;
  font-size: 30rpx;
  font-weight: bold;
  margin: 0 !important; /* 覆盖微信原生button组件默认margin */
  padding: 0;
}

.btn-save {
  background: #A41F35; /* 华师大红 */
  color: #FFFFFF;
  box-shadow: 0 8rpx 20rpx rgba(164, 31, 53, 0.2);
}

.btn-stop {
  background: #FFFFFF;
  color: #A41F35;
  border: 3rpx solid #A41F35;
}
@keyframes pulse {
  0% { opacity: 1; transform: scale(1); }
  50% { opacity: 0.4; transform: scale(0.8); }
  100% { opacity: 1; transform: scale(1); }
}